export declare type Server = "arc" | "cloudflare-workers" | "cloudflare-pages" | "express" | "fly" | "netlify" | "remix" | "vercel";
export declare type Lang = "ts" | "js";
interface CreateAppArgs {
    projectDir: string;
    lang: Lang;
    server: Server;
    install: boolean;
}
declare function createApp({ projectDir, lang, server, install }: CreateAppArgs): Promise<void>;
export { createApp };
