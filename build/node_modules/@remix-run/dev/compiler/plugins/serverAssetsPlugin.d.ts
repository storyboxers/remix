import type { Plugin } from "esbuild";
export declare type AssetsManifestPromiseRef = {
    current?: Promise<unknown>;
};
/**
 * Creates a virtual module of the asset manifest for consumption.
 * See {@link serverEntryModulesPlugin} for consumption.
 */
export declare function serverAssetsPlugin(browserManifestPromiseRef: AssetsManifestPromiseRef, filter?: RegExp): Plugin;
